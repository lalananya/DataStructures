public class MedianOfTwoSortedArrays {
     static void findMedianAndTwoSortedArrays(int[] arr1, int[] arr2, int n1, int n2){
        /* brute force, merge arrays O(n1 + n2), space complexity - O(n1 + n2) */
        /* no need to form the array it will save the space */
        
        int median = (n1 + n2 ) / 2; 

        /* there are two halves, : left half  :smaller  | right half : greater */
        median-1  median - even


     }
}
